#sample env file, rename the file to '.env' for it to work
MONGODB_URL=#Your MONGODB Local URL such as 'mongodb://localhost:27017' Note if this connection string does not work, change the 'localhost' into 127.0.0.1